# This crate lives inside a Go repo. Keep cargo's build output out of it.
# The documented commands (README, and the `rust` job in
# .github/workflows/examples-relay-signing.yml) all run cargo from this
# directory, so `target/` is created right here, next to Go source.
/target/
